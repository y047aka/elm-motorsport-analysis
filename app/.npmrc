# Every dependency in package.json is an exact version. `pnpm update` and
# `pnpm update --latest` already preserve that form; this covers `pnpm add`,
# which would otherwise write a `^` range for a package it has not seen before.
save-exact=true
