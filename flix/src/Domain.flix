/// `Domain` モジュールの親宣言。実体はサブモジュール
/// (`Domain.Car`, `Domain.Events`, `Domain.Metadata`, `Domain.RawLap`)
/// に分かれて `Domain/` ディレクトリ配下に配置される。
///
/// 親宣言が必要な理由は `Cli.flix` のコメント参照。
pub mod Domain { }
