pub mod Motorsport.Car {

    pub type alias Car = {
        carNumber    = String,
        drivers      = List[String],
        class        = String,
        group        = String,
        team         = String,
        manufacturer = String
    }

}
