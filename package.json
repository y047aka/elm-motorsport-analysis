{
  "type": "module",
  "scripts": {
    "start": "elm-land server",
    "build": "elm-land build --base elm-motorsport-analysis && mv ./dist/elm-motorsport-analysis/* ./dist/",
    "test": "elm-verify-examples && elm-test",
    "benchmark": "cd benchmark && elm reactor"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7",
    "elm-land": "0.20.1",
    "elm-test": "^0.19.1-revision12",
    "elm-verify-examples": "^6.0.3"
  }
}
