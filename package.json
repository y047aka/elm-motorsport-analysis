{
  "type": "module",
  "workspaces": ["app", "cli", "tests", "benchmark"],
  "scripts": {
    "start": "npm run -w app start",
    "build": "npm run -w app build",
    "test": "npm run -w app test",
    "benchmark": "cd benchmark && elm reactor"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7",
    "elm-test": "^0.19.1-revision12",
    "elm-verify-examples": "^6.0.3"
  }
}
