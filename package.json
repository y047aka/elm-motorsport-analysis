{
  "type": "module",
  "workspaces": ["app", "package", "cli"],
  "scripts": {
    "start": "npm run -w app start",
    "build": "npm run -w app build",
    "test": "npm run -w package test",
    "benchmark": "npm run -w package benchmark",
    "csv_to_json": "npm run -w cli build && npm run -w cli start"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7"
  }
}
